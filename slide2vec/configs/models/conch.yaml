csv:

output_dir:
save_previews: false

tiling:
  params:
    target_spacing_um: 0.5 # spacing at which to tile the slide, in microns per pixel
    target_tile_size_px: 448 # size of the tiles to extract, in pixels

model:
  level: "tile" # level at which to extract the features ("tile", "region" or "slide")
  name: "conch"

speed:
  fp16: true # use mixed precision during model inference
