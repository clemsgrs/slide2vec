csv:

output_dir: "output/debug"
tiling:
  preview:
    save: false
  params:
    target_spacing_um: 0.5
    target_tile_size_px: 224

model:
  level: "tile"
  name: "uni"

speed:
  precision: "fp16"
